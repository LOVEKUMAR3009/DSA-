package strings;

import java.util.HashMap;
import java.util.Map;

public class Frequency {
    // public static void main(String[] args) {
    //     String s = "aaabbc"; // Example input

    //     // Step 1: Count character frequencies
    //     Map<Character, Integer> map = new HashMap<>();
    //     for (int i = 0; i < s.length(); i++) {
    //         char key = s.charAt(i);
    //         map.put(key, map.getOrDefault(key, 0) + 1);
    //     }

    //     // Step 2: Group characters by their frequencies
    //     Map<Integer, Set<Character>> map2 = new HashMap<>();
    //     for (Map.Entry<Character, Integer> entry : map.entrySet()) {
    //         int count = entry.getValue();
    //         char character = entry.getKey();
            
    //         // Add character to corresponding frequency group
    //         map2.computeIfAbsent(count, k -> new HashSet<>()).add(character);
    //     }

    //     // Output result
    //     System.out.println(map2);
    // }
}


